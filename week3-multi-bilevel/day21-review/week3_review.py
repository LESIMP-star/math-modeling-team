#!/usr/bin/env python3
"""
week3_review.py — 第3周复习 (Day 21)
====================================
"""
import gurobipy as gp
from gurobipy import GRB

print("=" * 60)
print("第3周复习: 多目标 + 双层")
print("=" * 60)

# %% 复习1: 多目标二分法
print("\n[复习1] 请说出3种多目标处理方法的优缺点:")
print("""
| 方法       | 优点                  | 缺点                   |
|-----------|----------------------|------------------------|
| ① 权重法   | 最简单               | 漏非凸前沿上的点        |
| ② ε-约束法 | 不漏点, 更鲁棒        | 需要调网格密度          |
| ③ 分层法   | 优先级明确            | 低优先级目标可能很差    |
""")

# %% 复习2: 写一个简化的ε-约束
print("\n[复习2] 代码挑战: 写一个2目标ε-约束")
print("提示: 用下面的框架")
"""
m = gp.Model("review")
x = m.addVar(lb=0, ub=10)
y = m.addVar(lb=0, ub=10)
m.addConstr(x + y <= 12)

# 目标1: min x + y
# 目标2: min -x + 2*y

# 先用ε-约束法找5个帕累托点
"""
print("(在 my-practice/ 下自己建文件写)")

print("\n✅ 复习完第3周, 你应该能:")
print("  1. 手写ε-约束法循环")
print("  2. 把多目标下层嵌入KKT框架")
print("  3. 画帕累托前沿图")
